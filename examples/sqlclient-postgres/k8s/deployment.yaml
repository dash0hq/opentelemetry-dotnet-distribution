apiVersion: apps/v1
kind: Deployment
metadata:
  name: sqlclient-postgres
  namespace: dash0-dotnet-examples
  labels:
    app: sqlclient-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sqlclient-postgres
  template:
    metadata:
      labels:
        app: sqlclient-postgres
    spec:
      containers:
        - name: sqlclient-postgres
          image: localhost:5001/dash0-dotnet-example-sqlclient-postgres:latest
          imagePullPolicy: Always
          env:
            - name: POSTGRES_CONNECTION_STRING
              value: "Host=postgres;Port=5432;Username=postgres;Password=postgres;Database=postgres"
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
          livenessProbe:
            httpGet:
              path: /
              port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: sqlclient-postgres
  namespace: dash0-dotnet-examples
spec:
  selector:
    app: sqlclient-postgres
  ports:
    - port: 8080
      targetPort: 8080
